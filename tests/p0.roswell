_[Int 2] (Int -> Int) -> _[Int 2]
def map(array f):
  var result is _[Int 2]
  for z element in array:
    result[z] = f(element)
  return result

Int -> Int
def f(a):
  return *(a 8)

def main:
  var x = _[4 8]
  var y = map(x f)
